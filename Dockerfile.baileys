FROM node:20-slim

WORKDIR /app

# Install dependencies for Baileys
RUN apt-get update && apt-get install -y \
    git \
    && rm -rf /var/lib/apt/lists/*

# Copy package files
COPY package.json package-lock.json* ./

# Install all dependencies
RUN npm install

# Cache bust: change this value to force reinstall of Baileys
ARG BAILEYS_CACHE_BUST=v2_20260224

# Install Baileys-specific dependencies (latest for WA protocol compat)
RUN npm install @whiskeysockets/baileys@latest @hapi/boom qrcode pino

# Copy source code
COPY src/ ./src/
COPY tsconfig.json ./

# Create auth state directory
RUN mkdir -p /app/auth_state

# Expose port
EXPOSE 3001

# Health check
HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
  CMD curl -f http://localhost:3001/health || exit 1

# Start the Baileys bridge
CMD ["npx", "tsx", "src/server/baileys.ts"]
